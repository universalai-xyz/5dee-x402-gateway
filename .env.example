# ============================================================
# x402 Payment Gateway Configuration
# ============================================================
# Copy this file to .env and fill in your values.
# Only BASE_RPC_URL + SETTLEMENT_PRIVATE_KEY + PAY_TO_ADDRESS
# are required to get started.

# ─── Settlement Wallet ──────────────────────────────────────
# Private key for the wallet that submits transferWithAuthorization.
# This wallet ONLY pays gas — it does NOT receive USDC.
# Same key works across all chains.
# ⚠️  In production, use a secrets manager (GCP/AWS/etc.)
SETTLEMENT_PRIVATE_KEY=0x_YOUR_PRIVATE_KEY_HERE

# ─── Pay-To Addresses ──────────────────────────────────────
# Wallet that RECEIVES USDC payments. Same address works on all EVM chains.
PAY_TO_ADDRESS=0x_YOUR_PAYMENT_WALLET_HERE

# Route-specific overrides (optional, falls back to PAY_TO_ADDRESS)
# MY_PAY_TO_ADDRESS=0x...
# MY_PAY_TO_ADDRESS_SOL=...

# ─── RPC URLs ──────────────────────────────────────────────
# Only chains with an RPC URL will be advertised to agents.
# Settlement wallet needs gas tokens on each enabled chain.

# Primary (start with this — lowest fees)
BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_KEY

# High-liquidity chains (recommended)
# ETHEREUM_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY
# ARBITRUM_RPC_URL=https://arb-mainnet.g.alchemy.com/v2/YOUR_KEY
# OPTIMISM_RPC_URL=https://opt-mainnet.g.alchemy.com/v2/YOUR_KEY
# POLYGON_RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/YOUR_KEY
# AVALANCHE_RPC_URL=https://avax-mainnet.g.alchemy.com/v2/YOUR_KEY

# Additional chains (optional)
# UNICHAIN_RPC_URL=https://unichain.g.alchemy.com/v2/YOUR_KEY
# LINEA_RPC_URL=https://linea-mainnet.g.alchemy.com/v2/YOUR_KEY
# SONIC_RPC_URL=https://rpc.soniclabs.com
# HYPEREVM_RPC_URL=https://rpc.hyperliquid.xyz/evm
# INK_RPC_URL=https://rpc-gel.inkonchain.com
# MONAD_RPC_URL=https://rpc.monad.xyz

# Solana (optional — also requires SOLANA_FACILITATOR_PRIVATE_KEY)
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# SOLANA_FACILITATOR_PRIVATE_KEY=your_base58_private_key

# Facilitator-based chains
# MegaETH — USDM (18 decimals), settled via Meridian facilitator
# MEGAETH_RPC_URL=https://carrot.megaeth.com/rpc
# MERIDIAN_API_KEY=pk_...

# ─── Your Backend ──────────────────────────────────────────
# URL and API key for your backend service.
# The gateway proxies authenticated requests here.
MY_BACKEND_URL=https://api.your-service.com
MY_BACKEND_API_KEY=your_internal_api_key

# ─── Redis (required for replay protection) ────────────────
# Free tier at https://upstash.com works great
UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token

# ─── Optional ──────────────────────────────────────────────
# PORT=8080
# MY_PRICE=$0.01
# MY_PRICE_ATOMIC=10000

# ─── Credit System ─────────────────────────────────────────
# Credits compensate payers when their paid request settles
# on-chain but the backend returns an error (5xx). Disabled by default.
ENABLE_CREDIT_SYSTEM=false